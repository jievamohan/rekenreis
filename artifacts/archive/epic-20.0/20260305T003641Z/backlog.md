# Epic 20 — Backlog

> Generated by /epicify. Each micro-epic <= 5 tasks, each with a visible milestone.

---

## Micro-Epic Overview

| Slice | Title | Visible Milestone | Dependencies |
|-------|-------|-------------------|--------------|
| 20.1 | Playwright & Tokens Foundation | Playwright config + e2e smoke runs in container; new map/keypad tokens in tokens.css | None |
| 20.2 | Level Map Screen | /map page with winding path, nodes (locked/unlocked/completed), avatar, Play CTA | 20.1 |
| 20.3 | Play Screen Redesign (Keypad + ProblemCard) | /play uses numeric keypad instead of multiple-choice; ProblemCard + Check CTA | 20.1 |
| 20.4 | Level Complete Modal + Confetti | Modal with mascot, stars, confetti, Next CTA after level completion | 20.2, 20.3 |
| 20.5 | Mistakes Review + Polish | Friendly mistakes recap; level progress persistence; visual regression baselines | 20.4 |

---

## Task Estimates per Slice

### 20.1 — Playwright & Tokens Foundation (3 tasks)
1. Create `playwright.config.ts` + `e2e/` directory + smoke test
2. Add new design tokens (map path, keypad, node, CTA, easing)
3. Add `useLevelProgress` composable + `useMistakes` composable + schema extension

### 20.2 — Level Map Screen (5 tasks)
1. Create `pages/map.vue` + route + NavTabs update
2. `MapPath` component (SVG winding path with draw-in animation)
3. `MapNode` component (circle, star/lock states, interaction)
4. `MapAvatar` component + Play CTA integration
5. E2E: map smoke + visual screenshot baseline

### 20.3 — Play Screen Redesign (5 tasks)
1. `ProblemCard` component (big problem display)
2. `Keypad` component (0-9, clear, check)
3. Integrate Keypad + ProblemCard into play.vue (replace multiple-choice)
4. Play header redesign (score, progress, mascot label)
5. E2E: play smoke + keypad interaction + visual screenshot

### 20.4 — Level Complete Modal + Confetti (4 tasks)
1. `LevelCompleteModal` component (mascot, stars, Next CTA)
2. `Confetti` component (CSS-only, reduced-motion safe)
3. Level complete trigger logic (pack mode end-of-level detection)
4. E2E: level-complete modal + visual screenshot

### 20.5 — Mistakes Review + Polish (4 tasks)
1. `MistakesReview` modal/screen (card-based mistake recap)
2. Level progress persistence + star tracking integration
3. Accessibility audit (keyboard nav, contrast, reduced-motion)
4. Visual regression baselines for all screens + CI gate update

---

*End of Backlog*
