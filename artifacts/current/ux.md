# UX: Fix Failing Playwright Tests

No UX impact. Changes are:
1. Adding a `<title>` tag ("Rekenreis") — improves browser tab display (positive).
2. Generating visual regression baseline PNGs — no user-facing change.
